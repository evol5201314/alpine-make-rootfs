name: Build Alpine Rootfs with SSH and Python (Alpine container)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Alpine Linux 分支版本'
        required: false
        default: 'v3.20'
        type: string
      timezone:
        description: '时区'
        required: false
        default: 'Asia/Shanghai'
        type: string
      extra_packages:
        description: '额外安装的 Alpine 包（空格分隔）'
        required: false
        default: ''
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
      options: --privileged

    steps:
      - name: 安装必要工具和 alpine-make-rootfs
        run: |
          apk update
          apk add \
            alpine-make-rootfs \
            openssh python3 py3-pip \
            bash curl tar gzip xz git

      - name: 检出代码
        uses: actions/checkout@v4

      - name: 构建 Rootfs（预装 SSH + Python）
        run: |
          alpine-make-rootfs \
            --branch "${{ github.event.inputs.branch }}" \
            --arch aarch64 \
            --packages "openrc openssh python3 py3-pip ${{ github.event.inputs.extra_packages }}" \
            --timezone "${{ github.event.inputs.timezone }}" \
            --script-chroot \
            alpine-rootfs-${{ github.run_number }}.tar.gz - <<'SHELL'
              # 设置 root 密码为 "passwd"（请按需修改）
              echo 'root:passwd' | chpasswd

              # 启用 SSH 服务并设置开机自启
              rc-update add sshd default

              # 允许 root 通过 SSH 登录（密码方式）
              sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
              # 确保密码认证开启（默认就是 yes，以防万一）
              sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config

              # 安装 Python 包（示例）
              pip3 install --no-cache-dir --break-system-packages requests
          SHELL

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: alpine-rootfs
          path: alpine-rootfs-*.tar.gz
          retention-days: 7
